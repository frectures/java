import java.util.Iterator;

public class IteratorExercise {
    /**
     * @return alle gelieferten Elemente hintereinander geklebt, mit Komma dazwischen
     */
    public static String joinWithComma(Iterator<?> it) {
        return "";
    }

    /**
     * @return true, falls it1 die gleichen Elemente liefert wie it2
     */
    public static <E> boolean haveEqualElements(Iterator<E> it1, Iterator<E> it2) {
        return false;
    }
}
