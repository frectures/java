public class IterableExercise {
    /**
     * @return true, falls one die gleichen Elemente liefert wie two
     */
    public static boolean equalStrings(Iterable<String> one, Iterable<String> two) {
        return false;
    }
}
